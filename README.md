# POC: Redis (Redis Server & Redis Insight)

```bash
docker rm -f <name>
```

```bash
docker exec -it redis redis-cli
```

connection string:
`redis://default@redis-server:6379`

<!-- Anatomy -->
`redis://<user>:<password>@<hostname>:<port>`# poc-go-redis
